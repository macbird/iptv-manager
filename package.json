{
  "name": "iptv-manager-root",
  "private": true,
  "scripts": {
    "build": "npm run build -w packages/shared && npm run build -w apps/api && npm run build -w apps/web",
    "deploy": "./deploy/scripts/macbird-deploy-web.sh"
  },
  "workspaces": [
    "packages/*",
    "apps/*"
  ]
}
